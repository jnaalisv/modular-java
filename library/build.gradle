plugins {
    id "java"
}

sourceCompatibility = JavaVersion.VERSION_1_10

ext.moduleName = "library"

compileJava {
    inputs.property("moduleName", moduleName)
    doFirst {
        options.compilerArgs = [
            '--module-path', classpath.asPath,
        ]
        classpath = files()
    }
}
